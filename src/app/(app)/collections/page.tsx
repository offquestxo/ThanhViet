import { Library } from "lucide-react";
import { ComingSoon } from "@/components/app-shell/coming-soon";

export default function CollectionsPage() {
  return (
    <ComingSoon
      title="Collections"
      description="Real collection content lands in a later stage."
      icon={Library}
    />
  );
}
